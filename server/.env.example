# Porta do servidor Express
PORT=3005

# PostgreSQL — formato: postgresql://usuario:senha@host:porta/banco
DATABASE_URL=postgresql://postgres:senha@localhost:5432/dashboard

# JWT — gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=troque_por_segredo_aleatorio_longo

# Senha de acesso ao painel (login único)
ADMIN_PASSWORD=110agencia
